# ===================================================================
# Este arquivo serve como um template para os outros desenvolvedores.
# Cada um deve criar seu próprio 'application.yml' a partir deste.
# ===================================================================

# Configurações principais da aplicação
spring:
  application:
    name: ceasa-digital-api
  
  # Profile ativo - escolha um dos profiles disponíveis:
  # - test: Banco H2 em memória (para desenvolvimento rápido e testes)
  # - dev: Banco PostgreSQL (para desenvolvimento com dados persistentes)
  profiles:
    active: test  # Altere para 'dev' se quiser usar PostgreSQL
  
  # Configuração do JPA (aplicada a todos os profiles)
  jpa:
    open-in-view: false

# ===================================================================
# PROFILES DISPONÍVEIS:
# 
# 1. Profile TEST (application-test.yml):
#    - Banco H2 em memória
#    - Ideal para desenvolvimento rápido e testes
#    - Console H2 disponível em: http://localhost:8080/h2-console
#    - Não requer configuração adicional
#
# 2. Profile DEV (application-dev.yml):
#    - Banco PostgreSQL
#    - Ideal para desenvolvimento com dados persistentes
#    - Requer configuração do banco PostgreSQL
#    - Use o Docker Compose para subir o banco
#
# Para usar um profile específico, execute:
# mvn spring-boot:run -Dspring.profiles.active=dev
# ===================================================================